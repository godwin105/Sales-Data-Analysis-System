# =========================================================================
# Frontend (.env) — copy this file to .env and fill in your real values.
# Variables MUST start with VITE_ to be exposed to the React app.
# =========================================================================

# Base URL of the Flask API (no trailing slash).
# Default for local development matches the Flask dev server.
VITE_API_URL=http://localhost:5000
